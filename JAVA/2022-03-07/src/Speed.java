public class Speed {
    public static void main(String[] args) {
        int speed;
        speed = 10;
        System.out.println(speed);
        // 지역변수는 초기화를 해야한다.
        // 지역변수를 초기화하지 않으면 컴파일 오류가 난다.
    }
}
